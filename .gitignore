__pycache__/
*.pyc
.venv/
.env
.DS_Store
logs/
candidates.json
ranked.json
.rubric_hash
.claude/settings.local.json
coverage.xml
.coverage

# Astro
site/node_modules/
site/dist/
site/.astro/

# Old v0 artifacts (kept in repo as v0 archive but not actively published)
# (newsletters/ and index.md are in git history but no new commits should add to them)
